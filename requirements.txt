# Compatible release ranges; uv resolves versions for each OS and Python architecture.
# For reproducibility after first install, capture: uv pip freeze --python .venv/bin/python > requirements.lock.txt
"gymnasium[mujoco]>=1.1,<1.3"
"stable-baselines3>=2.6,<2.8"
"tensorboard>=2.16,<3"
"imageio>=2.34,<3"
"imageio-ffmpeg>=0.5,<1"
"matplotlib>=3.8,<4"
